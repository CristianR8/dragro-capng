import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dragro.app',
  appName: 'front-dragro',
  webDir: 'dist/front-dragro/browser',
};

export default config;
